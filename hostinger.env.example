# Hostinger → Deployments → Environment variables (só FRONTEND)
#
# Framework: Vite
# Build command: npm run build
# Output directory: dist
# Start command: (vazio — site estático)
#
# Cole EXATAMENTE esta linha (sem espaços, sem emoji, sem texto extra):
VITE_API_URL=https://sisfinance-api.up.railway.app/api
